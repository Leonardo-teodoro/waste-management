<tr>
  <td><%= active_session.user_agent %></td>
  <td><%= active_session.ip_address %></td>
  <td><%= active_session.created_at %></td>
  <td>
  <% if session[:current_active_session_id].to_i == active_session.id %>
    Sessão atual
  <% end %>
    <%= button_to "Sair", active_session_path(active_session), method: :delete, class:"btn btn-danger" %>
  </td>
</tr>
